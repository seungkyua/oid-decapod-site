apiVersion: openinfradev.github.com/v1
kind: HelmValuesTransformer
metadata:
  name: site

charts:
- name: argo-workflows-operator
  override:
    controller.resources.request.cpu: 500m
    controller.resources.request.memory: 1024Mi

